# Same database as workflow-1 and workflow-2
DATABASE_URL=postgresql://user:password@localhost:5432/civicreport

# JWT secret — must match the value used in workflow-2 (shared admin auth)
JWT_SECRET=your_super_secret_key_here

# Port — 5001 to match workflow-2 merged server
PORT=5001

NODE_ENV=development
